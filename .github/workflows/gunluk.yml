name: MarketPano gunluk

on:
  schedule:
    - cron: "0 9 * * *"        # her gun TR 12:00
  push:
    paths:
      - "borsa_local.json"
      - "hacim_local.json"
      - "kontrat_local.json"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  topla:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Paketleri kur
        run: pip install requests openpyxl

      - name: CMC verisi (ilk 150)
        run: python collect.py cmc

      - name: Pano hacmi - GitHub borsalari
        run: python collect.py github

      - name: Excel hacmi - her borsanin kendi ilk 150'si
        run: python collect.py hacim_github

      - name: Kontrat ozellikleri - GitHub borsalari
        run: python collect.py kontrat_github

      - name: Pano arsivi
        run: python collect.py arsivle

      - name: Excel arsivi (hacim + kontrat)
        run: python collect.py arsivle_yeni

      - name: Excel uret
        run: python build_excel.py

      - name: Kaydet
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cmc.json borsa_github.json hacim_github.json kontrat_github.json \
                  marketpano.xlsx manuel-kaldirac.csv arsiv/ || true
          if git diff --staged --quiet; then
            echo "Degisiklik yok."
          else
            git commit -m "Gunluk veri: $(date -u +%Y-%m-%d)"
            git push
          fi
